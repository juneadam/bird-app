// React module for the state/region component 

const State = (props) => {
    return (
        <div id="state">
            Region: <strong>{props.state}</strong>
        </div>
    )
}

export default State;