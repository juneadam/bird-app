// React module for the state/region component containing state data from JSON for bird dex

const State = (props) => {
    return (
        <div id="state">
            {props.state}
        </div>
    )
}

export default State;